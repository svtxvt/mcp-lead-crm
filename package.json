{
  "name": "mcp-lead-crm",
  "version": "0.1.0",
  "description": "A local MCP lead CRM with JSON/CSV storage and optional n8n webhooks.",
  "type": "module",
  "bin": {
    "mcp-lead-crm": "dist/index.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE",
    "SECURITY.md",
    "examples",
    "docs"
  ],
  "scripts": {
    "build": "node --eval \"require('node:fs').rmSync('dist',{recursive:true,force:true})\" && tsc",
    "dev": "tsx src/index.ts",
    "seed": "tsx src/seed.ts",
    "test": "npm run build && vitest run"
  },
  "engines": {
    "node": ">=20"
  },
  "author": "Sergey Yashchuk",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "tsx": "^4.23.13",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}
